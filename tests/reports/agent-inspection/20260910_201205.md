# Test Report: P4 Agent Inspection Cards

## Timestamp

2026-09-10 20:12:05 +09:00.

## Environment

Windows host, current `F:\dev\p4` debug agent build, P4 Studio Compose deployment on `http://127.0.0.1:43120`, Docker Desktop host route, connected Chrome browser.

## Executed Steps

1. Added and tested versioned event-v3 agent inspection request/snapshot media types in P4.
2. Built an inspection-capable `p4-agent.exe` and preserved the existing listener on `192.168.0.6:51054`.
3. Started the new build on `0.0.0.0:51055`, advertising `tcp://host.docker.internal:51055` for the Compose route.
4. Queried the local runtime directly through the P4 event wire.
5. Ran Studio typecheck, protocol/integration tests, production build, and documentation/boundary checks.
6. Deployed through `npm run deploy` and verified the machine-readable health report.
7. Registered `local-p4-agent` through the visible name/host/port form and opened the agent card.
8. Invoked the card-scoped P4 refresh, inspected the semantic tree and rendered card, and checked browser console errors.

## Observed Logs

- P4 protocol inspection constant test: 1/1 passed.
- P4 agent binary tests: 8/8 passed, including empty-agent machine/protocol snapshot.
- Direct snapshot: schema 1, event version 3, Windows, x86_64, 48 available cores, `llamacpp`, zero live nodes.
- Studio typecheck: 3/3 tasks successful.
- Studio tests: protocol 5/5 and integration 6/6 passed; integration includes successful/error observations and concurrency cap 2 across seven agents.
- Studio build: 2/2 tasks successful.
- Documentation inventory: 21/21 required root/app/package pages present.
- Package boundary: zero `packages` imports from `apps`.
- Deploy: `deploy-total status=ok service=studio port=43120 health=ok database=ready`.
- Final runtime: Studio container healthy; original P4 port 51054 and new port 51055 both listening.
- Browser card: `local-p4-agent`, reachable, 21 ms, P4 nodes 0, Windows/x86_64, available cores 48, `llamacpp`, event v3.
- Browser console errors: 0.

## Failures

- The first live request exposed an incorrect optional return-route encoding in the TypeScript fixture. The codec and fixture were corrected, then the direct P4 query and all tests passed.
- Direct Compose access to the Windows LAN address timed out. The one failed registration created during acceptance had no nodes and was removed; the agent was rebound to all interfaces and re-registered through the supported `host.docker.internal` route.
- The Codex in-app browser backend was unavailable. The connected Chrome surface completed registration, semantic inspection, and visual verification.
- P4's repository-wide docs lint remains RED on 28 pre-existing README-to-doc link errors. The modified event protocol document no longer adds a lint error.
- Vite retains the existing main-chunk size warning above 500 kB; the build succeeds.

## Reproducibility

Run `cargo test -p p4-agent --bin p4-agent` in `F:\dev\p4`. Start the built agent with explicit arguments `0.0.0.0:51055 tcp://host.docker.internal:51055`. In `F:\dev\p4studio`, run `npm run typecheck`, `npm test`, `npm run build`, and `npm run deploy`. Open `http://127.0.0.1:43120`, register `local-p4-agent` at `host.docker.internal:51055`, and open the agent view.

## Locator Outcome

The registration dialog exposed exactly one `에이전트 이름`, `호스트`, `포트`, and `등록` control with no agent adapter field. The final `agent-card` article was named `local-p4-agent`; its named `머신 정보`, `P4 등록 노드`, and `Studio 노드 선언` regions were independently discoverable. The `local-p4-agent P4 정보 새로고침` button was unique and refreshed the displayed event timestamp and latency.

Uncovered risk: the debug agent on port 51055 is a live process, not an installed Windows service, so a host reboot will stop it. Short-lived inspection connections also appear as `unexpected end of file` on the agent's connection-close log even after a successful reply.
