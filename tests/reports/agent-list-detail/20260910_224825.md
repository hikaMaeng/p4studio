# Test Report: Agent list, detail and registration editing
## Timestamp
2026-09-10 22:48:25 KST
## Environment
Windows host; Docker Studio `http://127.0.0.1:43120`; Chrome extension tab; live `local-p4-agent` at `host.docker.internal:51055`.
## Executed Steps
1. Ran Studio integration tests, workspace typecheck and production build.
2. Deployed with `npm run deploy --prefix F:\dev\p4studio`.
3. Opened a fresh Chrome-extension tab and selected the agent workspace.
4. Inspected the compact list, activated `local-p4-agent 상세 보기`, opened and cancelled registration editing, and returned to the list.
5. Reopened detail and saved the unchanged management name, host and port to exercise the deployed SQLite update and address reinspection path.
6. Read Chrome error logs after all transitions and the save.
## Observed Logs
- Studio integration: 8/8 passed.
- Turbo typecheck: 3/3 tasks passed.
- Turbo build: 2/2 tasks passed.
- Deploy: `deploy-total status=ok service=studio port=43120 health=ok database=ready`.
- Compact row exposed name/address, `RTX 3090 · RTX 4080`, RAM, P4 nodes, latency and reachability.
- Detail replaced the list and exposed SQLite registration, full RAM/GPU data and P4/Studio node sections.
- Saving unchanged registration moved latency from 6 ms to 5 ms and inspection time from 22:47:13 to 22:48:25, proving post-update reinspection.
- Chrome console errors: 0.
## Failures
None. Vite retained its existing bundle-size warning for the 536.67 kB main chunk.
## Reproducibility
Run the plan in `tests/plans/agent-list-detail-20260910.md` against a registered inspection-capable agent. Use a fresh Chrome URL when an older bundle is cached.
## Locator Outcome
`에이전트 목록` contained one `agent-row`; `local-p4-agent 상세 보기` uniquely opened `agent-detail`; the list was absent in detail; `등록정보 편집` exposed the uniquely labelled `관리 이름`, `호스트`, `포트` controls and `저장`; `에이전트 목록으로` restored the list. No positional CSS locator was required.
