# Test Report: Studio Foundation

## Timestamp

2026-09-10 19:05:56 +09:00.

## Environment

Windows host, npm 11.17.0, local production bundle on `http://127.0.0.1:43122`, disposable in-memory SQLite database, connected Chrome browser.

## Executed Steps

1. Ran `npm run typecheck` across the workspace.
2. Ran `npm test`, including legacy `agents.adapter` migration coverage.
3. Ran `npm run build` for the protocol package and Studio front/server bundles.
4. Started the production server bundle with an in-memory database.
5. Opened `에이전트 등록` and inspected its accessible controls and rendered layout.
6. Registered `browser-agent` using only name, host, and port, then verified the visible dashboard count and agent entry.

## Observed Logs

- Typecheck: 3/3 tasks successful.
- Tests: protocol 2/2 and Studio integration 4/4 passed.
- Build: 2/2 tasks successful; front and server artifacts emitted.
- Runtime: `p4studio status=ready port=43122 database=:memory:`.
- Dialog controls: `에이전트 이름`, `호스트`, `포트`, `취소`, `등록`; no agent adapter control.
- Registration result: dashboard count `01` and `browser-agent 127.0.0.1 : 59993 unknown` visible.

## Failures

- The Codex in-app browser backend was unavailable. The connected Chrome surface completed the same local checks.
- Vite emitted the existing warning that the main JavaScript chunk exceeds 500 kB; build still succeeded.

## Reproducibility

Run `npm run typecheck`, `npm test`, and `npm run build`. Start `apps/studio/dist/server/index.cjs` with `P4STUDIO_PORT=43122`, `P4STUDIO_SQLITE_PATH=:memory:`, and `NODE_ENV=production`, then open the local URL and register an agent.

## Locator Outcome

The dialog name `에이전트 등록` and labels `에이전트 이름`, `호스트`, `포트` were unique. The removed `기본 어댑터` label was absent. Registration completed without positional or CSS selectors and produced a positive visible replacement state.

Uncovered risk: the automatic column removal was tested against a representative legacy SQLite schema, not a copy of a production database.
