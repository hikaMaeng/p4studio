# Test Report: Studio Foundation

## Timestamp

2026-09-10 17:41:25 +09:00 through 17:43 KST.

## Environment

Windows host, Node 26.4.0 build runtime, npm 11.17.0, Docker Engine 29.7.2, deployed Node 24 Alpine container, `http://127.0.0.1:43120`.

## Executed Steps

1. `npm run typecheck`: 3 tasks successful.
2. `npm test`: protocol 2/2 and Studio integration 3/3 passed.
3. `npm run build`: protocol and Studio bundles completed.
4. Disposable demo runtime: opened dashboard, registered `acceptance-agent`, declared `acceptance-node`.
5. Browser accessibility inspection: one main, named navigation, dialogs and visible labels resolved.
6. Browser console inspection: 0 warning/error entries.
7. `npm run deploy`: image built, container recreated, published port health verified.
8. Deployed `/api/snapshot`: empty durable registry and P4 frame version 8 returned.

## Observed Logs

- `p4studio status=ready port=43120 database=data/p4studio.db`
- `deploy-total status=ok service=studio port=43120 health=ok database=ready`
- Docker publish: `0.0.0.0:43120->43120/tcp`, `[::]:43120->43120/tcp`
- Browser artifact: `tests/artifacts/studio-acceptance-20260910.png`
- Front bundle: 510.98 kB raw, 158.83 kB gzip.

## Failures

- Initial package install was interrupted before workspace links completed; lockfile-only plus completed install repaired it.
- Initial server ESM bundle failed on Express dynamic CommonJS require; Node 24 CJS bundle passed.
- Initial internal-only Compose network left the host port unpublished and blocked LAN agent reachability; explicit internal plus edge networks passed.
- A Windows `spawnSync` `.cmd` call returned `EINVAL`; fixed-command Windows shell invocation passed.

## Reproducibility

Run `npm run typecheck`, `npm test`, then `npm run deploy`. Verify `docker compose ps` and `GET http://127.0.0.1:43120/health`.

## Locator Outcome

Role/visible-name locators uniquely resolved `주 탐색`, `운영 개요`, `에이전트 등록`, form labels, and `노드 선언`. The repeated cards and topology expose approved test ids. No ambiguity or flakiness was observed. The in-app browser backend was unavailable; the connected Chrome surface completed the same checks.

Uncovered risk: full P4 binary status/command codec and real-agent protocol acceptance are not implemented in this foundation; reachability remains explicitly labeled as TCP-only.
