# Test Report: TTFT-based client TPS

## Timestamp
2026-09-14 20:34–20:50 KST

## Environment
Studio base b510af9fc06c893f247f26156c9f2a727b022092 + scoped working changes; P4 f80e360ac3e30049ddcc2f31591149e4586f32bf, unchanged. Windows PowerShell, Chrome extension tab 1233212405, http://localhost:43120/inference/query.

## Executed Steps
- Domain timing 5 tests: TTFT 100ms/10000ms with 50ms token spacing both yield 20 TPS; one token and zero interval yield null; request isolation and fractional timestamps pass; sampled terminal token included; legacy response preserved and rates converted.
- Mock WebSocket consumer test: two coalesced frames retain 100.25ms even when first consumer advances the clock by 500ms; fragmented frame uses final fragment time 1500.75ms.
- Typecheck, all 50 tests (protocol 6, domain 24, app 20), build and deploy passed. Chrome reload migrated existing results; a second reload retained responses and matching generation/final TPS.
- Fresh INSPECT attempted twice for both 122B agents; no response within 10 seconds. Read-only SSH checked processes and TCP backlog. No ready receipt was fabricated and no P4 process restarted.

## Observed Logs
Deployment output:
```text
result: status=ok services=studio compose=refreshed
time: total=34458ms resolve=687ms install=1ms build=7153ms compose=25904ms verify=711ms
verify: service=studio port=43120 health=ok
changed: dependencies=already-current,build=executed
```
Chrome evidence: [AX](../../../test/20260914/204500_ttft-tps/reloaded-ax.txt), [screenshot](../../../test/20260914/204500_ttft-tps/tps-result.png). Historical request `eb459e09-df4f-4813-892c-ea063602e099-1-1`: 256 sampled tokens, TTFT 574ms, generation/final TPS both 0.91 after migration. This is a converted historical observation, not a new benchmark.

## Failures
- Live 122B verification blocked: `INSPECT timeout`; both `192.168.0.26:52005` and `192.168.0.19:52005` show LISTEN Recv-Q=129, Send-Q=128. Agent/native PIDs still exist. This establishes backlog and missing replies, not the cause of the agent stall.
- Studio restart intentionally changed recorded ready deployments to unknown. Current stage load state could not be reconciled, so the model remains unavailable for new UI requests.
- Architecture check has an existing violation: `check name=exact-versions status=violation detail=non-exact-version-ranges` (`ws` and `@types/ws` ranges). Documentation checks pass. Vite large-chunk warning remains.
- Initial new mock test used an invalid opened control fixture (missing/invalid UUID agentId); corrected to match the public schema and rerun successfully.
- Browser event-loop and network delays remain in client TPS. Legacy rates cannot undo historical UI delays. No new engine TPS or 50-request performance improvement is claimed.

## Reproducibility
Follow [plan](../../plans/ttft-tps-20260914.md); run `npm run typecheck`, `npm test`, `npm run deploy`. Read-only agent diagnostic: `node test/20260914/204500_ttft-tps/inspect.mjs`. Actual new inference requires responding agents and verified current load state.

## Locator Outcome
Chrome AX model combo/table/request-ID rows and scroll worked without ambiguous matches. Agent selection list is empty because ready deployment count is zero after restart; this was confirmed by the model-deployments API status/error.
