# Node lifecycle verification report

Timestamp: 2026-09-16 12:09 KST. Result: local GREEN; actual P4 model acceptance not run.

## Scope and environment

- Studio HEAD at start: `b510af9fc06c893f247f26156c9f2a727b022092`. Existing dirty/untracked agent-group, inference, graph and UI work was preserved. Counts below describe the current worktree, not only this change.
- P4 source audited at `56c203b6d70dc91be46e399b6d68f85ebc53cda1`, initially clean. Later three untracked A-COST files appeared from concurrent work. This task did not modify/build/deploy P4 or operate remote agents.
- Windows / Node 26.4.0 / npm+Turbo; bundled Playwright with installed Chrome. Test app uses production frontend assets, memory SQLite, actual Studio Express API and opaque WebSocket→TCP bridge; the upstream Agent is a local protocol fixture, not a native model worker.
- Plan: [node lifecycle](../../plans/node-lifecycle-20260916.md). Contract: [node lifecycle](../../../docs/node-lifecycle.md).

## Commands and results

| Check | Result / evidence |
| --- | --- |
| `npm run typecheck` | PASS; [log](../../../target/node-lifecycle/typecheck-final.log) |
| `npm test` | PASS 114: protocol 18, domain 63, app 33; [log](../../../target/node-lifecycle/tests-final.log) |
| `npm run build` | PASS; [log](../../../target/node-lifecycle/build-final.log). Existing large frontend chunk warning remains. |
| architecture / i18n / docs scripts | PASS, 0 violations; docs includes app. Existing architecture crowding/wire-type warnings remain. |
| changed Markdown local links | PASS, 0 missing links |
| `git diff --check` | PASS; line-ending normalization warnings only |
| initial browser smoke | PASS at `http://127.0.0.1:51118`; [smoke report](../../../test/20260916/120000_node-lifecycle/smoke/report.md) |
| final browser scenario | PASS at `http://127.0.0.1:57268`; [result](../../../test/20260916/120000_node-lifecycle/report.json), [wire](../../../test/20260916/120000_node-lifecycle/wire.json) |

## Observed behavior

- Browser planned two stages on one agent without pre-created empty nodes. The pre-existing observed node had no connection handles and received no lifecycle command.
- All 11 lifecycle messages targeted Agent endpoints: 6 LOAD and 5 UNLOAD. CREATE/DELETE count 0. Stable node IDs included `/`; new node/load generations survived HTTP receipts, reload and subsequent UNLOAD.
- Normal LOAD→UNLOAD→reLOAD→UNLOAD completed. Partial LOAD failure preserved `fixture native load failed` and unloaded the successful sibling.
- Final fixture task-owned nodes 0; protected pre-existing node 1. Browser page/console errors 0.
- Active model edit deep URL was blocked. Model detail reload, back/forward and retired standalone-node URL→model placement navigation passed.
- Domain counterexamples cover duplicate-ID no-touch, busy UNLOAD with later recovery, wrong node/generation/operation/adapter/source/content/resource, invalid metadata, wrong opaque load generation, lost replies, unknown LOAD followed by absent UNLOAD, build incompatibility and persistence failure.
- Protocol fixture is the literal Python canonical payload consumed by current Rust `lifecycle.rs`; byte prefix and opaque body are compared. This task did not recompile Rust.

## Visual and locator review

Inspected every final scenario screenshot: [planned stages](../../../test/20260916/120000_node-lifecycle/screenshots/planned-stages.png), [loaded](../../../test/20260916/120000_node-lifecycle/screenshots/loaded.png), [unloaded](../../../test/20260916/120000_node-lifecycle/screenshots/unloaded.png), [partial failure and recovery](../../../test/20260916/120000_node-lifecycle/screenshots/partial-failure-recovered.png).

Role/name, label, article accessible names and documented flow-agent/flow-node/model-node-inspector test IDs worked. CSS was used only to count ReactFlow handles on the protected node. Required MUI labels include `*`; assertions use the actual visible labels. Error text inside Alert shares an element with its back button, so the locator scopes the alert by text.

## Failures and evidence boundaries

First pass was not GREEN. Initial typecheck exposed an obsolete server deployment socket import after the old runner was removed; that unused socket was retired and dial routing is now tested as opaque bytes. One new connection fixture supplied a non-UUID connection ID and timed out; it was corrected to the existing transport schema.

Five initial scenario attempts failed in the test harness: required-label matching, a URL wait that matched `/models/new`, `Ready` vs actual `Loaded`, exact Alert text including its child button, and `Failed` vs actual `Failed · inspect reports`. These did not establish end-to-end PASS. The final corrected scenario completed all assertions in one run. Available earlier failure evidence is retained alongside the final report as `attempt2`–`attempt5` files. No assertion or runtime success requirement was removed.

Actual P4 agent/native LOAD, HF-specific adapter payloads, GPU memory release and multi-machine inference were not executed here. A permanently unobserved LOAD remains explicitly unknown and blocks reuse; there is no invented global recovery service or unbounded retry loop. Existing saved declaration/pipeline records remain historical metadata.

## Reproduction

Build the current worktree, then run `node --import tsx test/20260916/120000_node-lifecycle/server.mts` in one terminal. It writes its fresh ephemeral URL/agent identity to `server.json`. Run `node test/20260916/120000_node-lifecycle/browser.mjs` in another terminal. Each scenario needs a fresh fixture database. Stop only the fixture PID recorded in `server.json`; final verification fixtures were stopped after evidence capture. Current production service was not refreshed.
